import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vue3_ts_wx_template',
  appName: 'vue3_ts_wx_template',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
